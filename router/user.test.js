const app = require('../app'); // this is the Express app instance
const request = require('supertest')(app);

  test ('should return a list of all users', (done) => {
  })

  test('GET /users/:id/shows', () => {
    it('all shows watched by a user', (done) => {
    });
  });